import { types } from './{{ name }}.actions';

const initialState = {};

function {{ name }}Reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default {{ name }}Reducer;
