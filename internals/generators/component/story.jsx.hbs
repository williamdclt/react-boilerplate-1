import React from 'react';
import { storiesOf } from '@storybook/react';
import {} from '@storybook/addon-knobs';
import {{ name }} from '../{{ name }}';

storiesOf('{{ name }}', module).add('{{ name }}', () => <{{ name }} />);
